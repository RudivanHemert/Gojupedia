interface AudioButtonProps {
  text: string;
  translationKey?: string;
  lang?: string;
  className?: string;
  size?: 'sm' | 'md' | 'lg';
}

const AudioButton = (_props: AudioButtonProps) => null;

export default AudioButton;
